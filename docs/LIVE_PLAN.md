# Live forecasting integration

User request: fetch open weather APIs now at turbine 1 (43.645150, 78.535604) and turbine 2 (43.643198, 78.538828), predict power and energy continuously, integrate the team's physics code and chart, use GPT-6 Luna high agents and commit project changes to GitHub.

User supplied rated power: 2.5 MW. Configure 2.5 MW per turbine (5 MW total), label as user supplied and keep editable configuration. Do not use earlier synthetic 3.2 MW value.

Branch evidence: main at 0fe607f453a8c804733f9c9b4dcf81b3295b9534 has src/physics.py. Physics branch Medina150207-feature/physics at 2c012428ff9077483c7109484efa38c6455cfbe2 has power_curves_turbines.png and analysis notebook. FRONT branch is an old README-only branch. Preserve teammate contributions and use an integration branch for live work.

## Responsibilities and contracts

Project: outputs/wind-agent. Development Python: ../../work/venv/Scripts/python.exe from project root. No repo commits by agents; supervisor publishes and reviews. Do not modify other agents' files without coordination.

### Weather agent
Own windagent/live_weather.py and tests/test_live_weather.py. Keep historical weather.py unchanged.
`fetch_live_weather(turbine_id: int, horizon: int, cache_dir: Path, refresh: bool=False, *, forecast_start=None, now=None) -> dict`.
Return keys `frame` (DataFrame with UTC-aware timestamp, wind_speed m/s at100m, temperature C), `current` (modelled current conditions and their valid timestamp, wind10m if requested and current-hour100m as separate explicit fields), `provenance` (source, requested/returned coordinates, retrieved_at UTC, source_hash, model, initialization_time null if API doesn't disclose, wind_height_m=100, cache age/status). Use Open-Meteo live forecast API explicit ECMWF IFS and UTC. No manufactured initialization/publication time. Frame has EXACT horizon rows starting forecast_start, default next full UTC hour from now. horizon24–48. Return a fresh snapshot, TTL300s with refresh forcing acquisition; no silent stale cache fallback. Validate full horizon, units, finite values, current validity, cache integrity and age. Use bounded retries, atomic raw snapshot writes. Actual fetch and save fixture evidence now, no code or tests depend on constant current date.

### Backend/physics agent
Own windagent/live.py, minimal additions api.py, tests/test_live.py, tests/test_live_api.py, config/turbines.json. Integrate src.physics.apply_physics_sanity_check explicitly at validation in the live pipeline. Existing replay agent can receive physics integration only if backwards behavior and tests are intentionally maintained; coordinate supervisor. Forecast using existing trusted models; no schema change without retraining. Expose `LiveForecastAgent(home).run(horizon=48,refresh=False) -> dict`. Weather import from above. Return issued_at actual UTC after acquisition, forecast_start next fullUTC hour, model metadata and training age, per-turbine coordinates/current/provenance/forecast points with normalized_power, power_mw, energy_mwh for1h, lower/upper in consistent physical bounds, summary energy_24h_mwh/energy_horizon_mwh, and farm sum plus totals. Config 2.5MW each, source user. Metadata tells current conditions are model estimates, not live SCADA observations; historical model available Jan31 age matters. Apply team sanity function to point predictions and interval bounds; log corrections and preserve raw values. Actual future energy is forecast, never measured. Store durable audited runs via existing ForecastStore or separate live DB; hash cache key includes model/config/physics implementation and weather content. Reuse unchanged outputs but preserve original forecast-issued timestamp and new check timestamp. No future forecast_start before issued time (recheck hour boundary). API GET /api/live?hours=48&refresh=false and /api/live/status; clear errors503 no mock fallback. Serve dashboard static root and files explicitly, not entire data tree. Autonomous polling every300s in optional FastAPI lifespan/config or CLI supervisor; default local server should keep latest forecasts updated with one background loop and bounded refresh, no network on import or hidden errors.

### Frontend agent
Own app.js,index.html,styles.css only and optional frontend test file. Keep design/controls where useful, replace synthetic genForecast entirely with GET /api/live. Current date/live source status, 24/48 horizon, turbine selection, actual MW/MWh using backend2.5MW, UTC chart with source retrieval/forecast issue timestamps, loading/error/stale state, periodic refresh (300s), AbortController request races handled. API schema coordinate directly backend agent. No mock fallbacks, no synthetic historical dates in live mode. Historical replay remains separate API/docs. Use existing chart image /assets/power_curves_turbines.png (supervisor fetches). If backend unavailable show actionableerror and last successful data explicitly stale. No external shared state changes; browser UI tests supervisor.

### Supervisor
Review integration, acquire physics-branch chart and preserve provenance, add live CLI/start instructions, test API and actual fresh forecast with current time and real network, browser-check dashboard, save live report and ZIP, commit to integration branch and main as appropriate preserving intervening teammate work. Provide real computed energy/power and timestamps to user; do not imply forecasts are measured generation or a guaranteed always-on hosted service.
